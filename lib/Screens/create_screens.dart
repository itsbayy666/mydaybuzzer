import 'package:flutter/material.dart';

class createscreens extends StatelessWidget {
  const createscreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("createscreens"),
      ),
      body: Container(
        color: Colors.orange,
      ),
    );
  }
}
